export interface StudyPlace {
  id: number;
  osm_id: number,
  name: string,
  latitude: number,
  longitude: number,
  status: string,
  avg_rating: number,
  wifi_speed: string;
  noise_level: string;
  power_availability: string;
  place_type: string;
  working_hours: string;
  created_at: string;
}

